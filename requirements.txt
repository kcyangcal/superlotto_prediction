# Python 3.12 環境
# ── 網路抓取 ──────────────────────────────────────────────────────────────────
requests==2.32.3        # HTTP 客戶端，含 Session 與 Retry 機制
beautifulsoup4==4.12.3  # HTML 解析器（教學用 fallback，主要爬蟲使用 requests JSON API）
lxml==5.3.0             # BeautifulSoup4 的高效能解析後端

# ── 資料處理 ──────────────────────────────────────────────────────────────────
pandas==2.2.3           # 資料框操作、特徵工程、讀取 SQLite 結果
numpy==1.26.4           # 數值計算基礎（pandas/sklearn 的底層依賴）

# ── 機器學習 ──────────────────────────────────────────────────────────────────
scikit-learn==1.5.2     # RandomForestClassifier、MultiOutputClassifier、評估指標
xgboost==2.1.3          # 梯度提升樹（作為 Random Forest 的對照實驗）

# ── 視覺化 ────────────────────────────────────────────────────────────────────
matplotlib==3.9.2       # 基礎繪圖（頻率直方圖、特徵重要性圖）
seaborn==0.13.2         # 統計視覺化（熱圖、分佈圖）

# ── Jupyter 環境 ──────────────────────────────────────────────────────────────
jupyter==1.1.1          # Notebook 環境（EDA 探索分析）
ipykernel==6.29.5       # Jupyter 的 Python kernel

# ── 開發工具 ──────────────────────────────────────────────────────────────────
pytest==8.3.3           # 單元測試框架
python-dotenv==1.0.1    # 環境變數管理（若未來需要 API key 等敏感資訊）

# ── 選用：深度學習 (LSTM 模型) ────────────────────────────────────────────────
# 若要實作 L3 LSTM 模型，請解除以下註解後重新執行 pip install -r requirements.txt
# torch==2.4.0           # PyTorch（LSTM 時序模型）
